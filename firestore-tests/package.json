{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator tests for the hardened Firestore security rules (firestore.rules.hardened). Run BEFORE deploying the hardened rules.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-foodyzz \"node rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2"
  }
}
