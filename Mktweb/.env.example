# ── Site ──────────────────────────────────────────────────────────────────────
# Public base URL (used for canonical/OG tags + the download QR target).
# No trailing slash. Inlined at build time (static export).
NEXT_PUBLIC_SITE_URL=https://mkt.unihamper.com

# Note: this is a STATIC site (output: "export"). There are no server secrets.
# The contact form posts to Formspree — set the form ID in src/lib/siteConfig.ts.
